<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>

          <%= link_to "Contact App", root_path, class: "navbar-brand text-uppercase" %>          
    </div>

        <div class="collapse navbar-collapse" id="navbar-collapse">
          <div class="nav navbar-right navbar-btn">
          <% if user_signed_in? %>
            <ul class="navbar-nav ml-auto">
              <a><%= link_to "Log Out", destroy_user_session_path, method: :delete, :class => 'btn bnt-secondary' %></a>
            </ul>
          <% else %>
            <div class="navbar-text ml-lg-3">
              <ul class="navbar-nav ml-auto">
              <a> <%= link_to("Log In", new_user_session_path) %></a> &nbsp;&nbsp;
              <a> <%= link_to("Sign Up",  new_user_registration_path) %></a>
              </ul>
            </div>
          <% end %>
    </div>
   </div>
 </div>
</nav>